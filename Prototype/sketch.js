function setup() {
    let canvas = createCanvas(800, 500);
    canvas.parent("p5-canvas-container");
    rectMode(CENTER);
    colorMode(HSB, 360, 100, 100);
}

function draw() {
  background(0, 0, 20);
}


